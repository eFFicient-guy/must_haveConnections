`GCC 15` - accelerates software performance and development by enforcing modern language standards by default, introducing incremental build optimizations, and adding deep native hardware support for next generation processors.

`GCC 15` - natively handles standard c++23 modules, that means it compiles modern features much faster and requires less setup.